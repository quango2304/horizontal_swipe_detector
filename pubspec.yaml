name: horizontal_swipe_detector
description: A new Flutter project.
version: 0.0.2
homepage: https://github.com/quango2304/horizontal_swipe_detector

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
